<div class="showpage">
  <h1> This is the showpage of:</h1>
  <%= @offer.name %>
  <%= @offer.description %>
  <%= @offer.category %>
  <%= @offer.offer_category %>
  <%= @offer.price %>
  <%= @offer.start_date %>
  <%= @offer.end_date %>
  <%= link_to "Delete", offer_path(@offer), method: "delete", data: { confirm: "Are you sure?" }%>
</div>
